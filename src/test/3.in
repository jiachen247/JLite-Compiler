/* This test ensure lexical analyzer is working as intended with the JLite specification*/
class Main {
    Void main() {
        // identifiers should start with a lower letter
        Int aABCD_aasckln123;
        String string;
        Bool bool;

        aABCD_aasckln123 = 1234567890;
        string = "hello world";
        bool = false;
        bool = true;


        // binary operators
        // boolean operators
        uninitialized = hello && world || true;


        // relational operators
        id = 1 < 2 || 1 > 2 || 1 <= 2 || 1 >= 3 || 1 != 1 || 1 == 1;

        // arithmetic operators
        id = 1 + 2 * 3; // precedence rules
        id = 1 + 1 - 2 * 3 / 4;

        // can even divide by zero since there's no semantic analyzer yet
        id = 1 / 0;

        // unary operators
        id = -1;
        id = !abc;
        id = !true || !false;

        id = "string 1" + "string 2" + atom1;

        // single line comment

        /*
            multi line comment goes here

        */


        return ;
    }
}

// cname should start with an upper case letter
class Dummy_123 {
    Void get() {
        return 1;
    }
}
