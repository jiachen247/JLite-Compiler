/* Failure case for type check */

class Main {
    Void main(){
        return 1;
    }
}

class Dummy {
    Int i;
    Bool b;
    String s;

    Int test(Int a) {
        return 2;
    }
    Int retInt() {
        return i;
    }

    Bool returnBool() {
        return b;
    }

    String retString() {
        return s;
    }

    Void retVoid() {
        return ;
    }

}

class TestStatements {
    Void test() {
        Int x;
        String s;
        Bool b;
        Dummy d;

        d = new Dummy();

        if (1 + 1) {
            d.retInt();
        } else {
            d.retBool();
        }

        while(s) {
            return s;
        }

        x = 1 + 1 * 2;
        x = "1234" - 5;

        b = x<-x;
        s = s + "abc";
        b = !b || false;

        d.retInt();
        method1();
    }

    Void expr () {
        Int x;
        String s;
        Bool b;
        Dummy d;



        // BinaryExpression

        return ;
    }
}

class TestExpressions {
    Void test() {
        Int x;
        String s;
        Bool b;
        Dummy d;

        d = new Dummy();

        // Illegal Unary Expressions
        x = -(1 > 2);
        b = !(1 + 2);

        // Illegal Boolean Expressions
        s = "pwd" + 1;
        s = s * s;

        // Illegal new expression
        d = new UndefinedClass();
        x = new Dummy();

        // Undefined variables
        x = undefinedVariable;
        x = undefinedVariable.x;
        x = d.undefinedVariable;
        x = d.undefinedMethod();
        x = (test())();

        // Test call expressions
        x = d.test(undefined);
        x = d.test(1, 2, 3);
    }
}
