/* Failure case for type check */

class Main {
    Void main(){
        return 1;
    }
}

class Dummy {
    Int retInt() {
        return 1;
    }

    Bool returnBool() {
        return true;
    }

    String retString() {
        return "";
    }

    Void retVoid() {
        return ;
    }
}

// (A) No two fields in a class can have the same name.
class TestA {
    Int a;
    Bool b;
    String c;

    Void method1() {
        return ;
    }

    Int invalidReturn() {
        return ;
    }

    Int return1() {
        return 1;
    }

    Void stmt() {
        Int x;
        String s;
        Bool b;
        Dummy d;

        d = new Dummy();

        if (1 + 1) {
            d.retInt();
        } else {
            d.retBool();
        }

        while(s) {
            return s;
        }

        x = 1 + 1 * 2;
        x = "1234" - 5;

        b = x<-x;
        s = s + "abc";
        b = !b || false;

        d.retInt();
        method1();
    }

    Void expr () {
        return ;
    }
}
