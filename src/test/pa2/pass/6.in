/* Pass test for IR Gen */

class MainC {
    Void main (){
        return;
    }
}

class Test {
    Int a;
    Test t;

    Int f(Int b) {
        if ( (a < 2) || (a > 2)) {
            return 1;
        } else {
            return 3;
        }
    }

    Bool f(Bool b) {
        a = 0;

        while (!(a + 2 < 10)) {
            a = a + 1;
        }

        return true;
    }

    Int g(Int b) {
        return 3;
    }

    Int h(Int b) {
        b = 1+ 1 + 1+ 1;
        return 3;
    }

    Void g() {

        Test x;
        // t.t.t.a = 1;
        // t.f(1);
        // t = null;

        x = new Test();
        t.t = new Test();
        // t = t.getTest();
        return ;
    }

    Test getTest() {
        return new Test();
    }
}