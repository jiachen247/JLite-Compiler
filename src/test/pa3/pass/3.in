/* Test function call */

class Mainnnn {
    Void main() {
        Dummy d;

        d = new Dummy();
        println(d.fun1(1, 2, 3));
        println("\n");
        println(d.fun2(1000, 2, 3, 4, 5, 6, 0, 0));
    }
}

class Dummy {
    Int fun1(Int a, Int b, Int c) {
        return a + b + c;
    }

    Int fun2(Int a, Int b, Int c, Int d, Int e, Int f, Int g, Int h) {
        return  a - b * c + d * e + f + g + h;
    }
}