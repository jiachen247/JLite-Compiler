/* Test function call */

class Mainnnn {
    Void main() {
        Dummy d;
        Int c;

        d = new Dummy();
        println(d.fun1(1, 2, 3));
        c = 1;
        println(d.fun2(1000, c, 3, 4, 5, 6, 0, 0));
    }
}

class Dummy {
    Int fun1(Int a, Int b, Int c) {
        return a + b + c;
    }

    // 1000 - 1 * 3 + 4 * 5 + 6 + 0 + 0 = 1023
    Int fun2(Int a, Int b, Int c, Int d, Int e, Int f, Int g, Int h) {
        return  a - b * c + d * e + f + g + h;
    }
}