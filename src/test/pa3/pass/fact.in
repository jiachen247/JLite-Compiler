class Main {
    Void main() {
        Factorial factorial;

        factorial = new Factorial();
        println(factorial.fact(5));
    }
}

class Factorial {
    Int fact(Int n) {
        if (n == 1) {
            return 1;
        } else {
            return n * fact(n - 1);
        }
    }
}
