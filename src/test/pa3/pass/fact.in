class Main {
    Void main() {
        Factorial factorial;

        factorial = new Factorial();
        println(factorial.fact(10, 10,10,10,10));
    }
}

class Factorial {
    Factorial f;
    Int a;

    Int fact(Int k, Int j, Int n, Int a, Int b) {
        if (n == 1) {
            return 1;
        } else {
            return n * fact(10, 10,n - 1,10,10);
        }
    }
}
